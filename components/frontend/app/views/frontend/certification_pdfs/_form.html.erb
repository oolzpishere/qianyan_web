<%= form_with(model: certification_pdf) do |form| %>
  <% if certification_pdf.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(certification_pdf.errors.count, "error") %> prohibited this certification_pdf from being saved:</h2>

      <ul>
        <% certification_pdf.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.submit %>
  </div>
<% end %>
