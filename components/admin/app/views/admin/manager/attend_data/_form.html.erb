<%= form_with(model: manager_attend_datum) do |form| %>
  <% if manager_attend_datum.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(manager_attend_datum.errors.count, "error") %> prohibited this manager_attend_datum from being saved:</h2>

      <ul>
        <% manager_attend_datum.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.submit %>
  </div>
<% end %>
