
<div class="container mx-auto w-4/5">
  <h3> <%= @attend_datum.conference.name %> </h3>

  <%= button_to '更新datum',
     "/manager/attend_data/update_datum/#{@attend_datum.id}",
      data: {turbo_method: :post}, class: " btn-sm btn-primary" %>

  <table class="table-striped-gray">
    <!-- <thead>
      <tr>
        <th colspan="3"></th>
      </tr>
    </thead> -->

    <tbody>
      <% @attend_datum.attributes.each do |k, v| %>
        <tr>
          <td><%= k %></td>
          <td><%= v %></td>
        </tr>
      <% end %>
    </tbody>
  </table>


  <%# link_to 'Edit', edit_manager_conference_path(@manager_conference) %>
  <%= link_to '返回', manager_attend_data_select_path, class: "btn btn-sm btn-primary "%>


  <p></p>
</div>